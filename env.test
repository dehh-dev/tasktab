# Ambiente usado pelos testes automatizados (banco separado, truncado a cada teste).

NODE_ENV=test
PORT=3001

DB_HOST=localhost
DB_PORT=5432
DB_USER=tasktab
DB_PASSWORD=tasktab
DB_NAME=tasktab_test

DATABASE_URL=postgres://tasktab:tasktab@localhost:5432/tasktab_test
