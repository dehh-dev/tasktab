{
  "name": "tasktab",
  "version": "1.0.0",
  "description": "Gerenciador de Tarefas - API REST em arquitetura MVC",
  "main": "src/server.js",
  "engines": {
    "node": ">=24.18.0"
  },
  "scripts": {
    "dev": "npm run services:up && npm run services:wait:database && npm run migrations:up && node --watch src/server.js",
    "start": "node src/server.js",
    "services:up": "docker compose up -d",
    "services:stop": "docker compose stop",
    "services:down": "docker compose down",
    "services:wait:database": "node scripts/wait-for-postgres.js",
    "migrations:up": "node-pg-migrate --envPath env.development up",
    "migrations:down": "node-pg-migrate --envPath env.development down",
    "migrations:create": "node-pg-migrate create",
    "seed": "npm run services:up && npm run services:wait:database && npm run migrations:up && node seeds/seed.js",
    "pretest": "npm run services:up && cross-env NODE_ENV=test npm run services:wait:database",
    "test": "cross-env NODE_ENV=test jest --runInBand --verbose",
    "posttest": "npm run services:stop",
    "test:watch": "cross-env NODE_ENV=test jest --watchAll --runInBand --verbose",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "keywords": [
    "tasks",
    "crud",
    "express",
    "postgres",
    "mvc"
  ],
  "license": "MIT",
  "overrides": {
    "brace-expansion": "5.0.8",
    "glob": "^11.1.0"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "eslint": "^9.17.0",
    "eslint-config-prettier": "^9.1.0",
    "globals": "^15.14.0",
    "jest": "^29.7.0",
    "node-pg-migrate": "^7.9.1",
    "prettier": "^3.4.2",
    "supertest": "^7.0.0"
  }
}
